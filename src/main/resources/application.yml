spring:
  datasource:
    url: jdbc:mysql://localhost:3306/EcomAzanesPaul?createDatabaseIfNotExist=true
    username: root
    password: Zaqmlp321!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    user:
      name: user
      password: password